class Solution {
    public int maxSubArray(int[] nums) {
        int pre = 0, res = nums[0];
        for(int x : nums){
            res = Math.max(res,pre = Math.max(pre+x, x));
        }
        return res;
    }
}